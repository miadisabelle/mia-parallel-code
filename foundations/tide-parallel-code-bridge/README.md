# Foundations — Tide ⇄ Parallel Code Bridge

> A cross-repository `deep-research-foundations` packet capturing the intention to bridge Miadi's
> **`tide`** runtime with **Parallel Code's** phone-steerable remote — so a future agent landing on
> a related issue arrives already oriented, and the whole thread can weave into a Chronicle episode.

- **Generated:** 2026-07-23
- **Subjects:** `miadisabelle/mia-parallel-code @ 0bc5806` (remote runtime) · `jgwill/Miadi` (tide contract/client/cockpit)
- **Method:** two parallel first-hand code investigations → cross-repo synthesis

## The recognition in three sentences

1. Parallel Code has a **working, phone-reachable terminal-steering runtime** but speaks no relational
   contract — and its Connect-Phone QR hands out a **LAN address unreachable over Tailscale**.
2. Miadi's **`tide`** has a **schema-first contract and a cockpit** but its steering is **gated behind
   an interactive TTY**, so the web/remote path refuses to act ("didn't talk to her").
3. Each holds what the other lacks; a **`parallel` Multiplexer transport** in `@miadi/tide` lets the
   working runtime satisfy tide's contract — and Parallel Code's Tailscale auth dissolves tide's
   TTY blocker.

## What's in this packet

| File                       | What it holds                                                                                                                                       |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| `context-layer.md`         | Canonical terms, the cross-repo relationship graph, label conventions, related issues (full refs), placement/federation                             |
| `intent-understanding.md`  | The intention, the structural tension, audiences, decisions, and weave-ready lineage edges                                                          |
| `bridge-design.md`         | **The enumerated integration/extension aspects (A0–A10)** — each an issue-sized unit with a precise landing point (repo + file + seam + acceptance) |
| `cross-repo-issue-plan.md` | Draft issues (per repo, labels, full cross-refs) mapping 1:1 onto the aspects — ready to create on William's go                                     |
| `source-ledger.yaml`       | Every claim → source in both repos                                                                                                                  |

## For the next agent (start here)

1. Read `bridge-design.md` → the **Aspect map** table. Pick the aspect your issue names (A0–A10).
2. Each aspect tells you the repo, the file, the seam, and the acceptance signal.
3. **A0** (Tailscale-reachable QR) is the first stone — smallest, unblocks phone access today.
4. **A2 → A3 → A4/A5/A6** is the contract-first bridge (Direction A), reusing the herdr pattern.
5. Cross-link with full `owner/repo#number`; label `miadi-chronicle` (this repo) / `inquiry`,`specs` (Miadi).

## Deferred (session-close ceremony)

`@miadi/inquiry-weave relate` binds {this packet · the issues · a Chronicle episode}; then `register`
federates it. Aspect **A10** — do not run early.
